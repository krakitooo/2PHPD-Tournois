{% extends 'admin/header.html.twig' %}

{% block title %}Dashboard Admin{% endblock %}

{% block body %}
    <h1>Dashboard Administrateur</h1>

    <nav>
        <a href="{{ path('admin_dashboard') }}">Dashboard</a>
        <a href="{{ path('admin_players_index') }}">Joueurs</a> 
        <a href="{{ path('admin_tournaments_index') }}">Tournois</a> 
        <a href="{{ path('admin_registrations_index') }}">Inscriptions</a> 
        <a href="{{ path('admin_sportmatches_index') }}">Matchs</a>
    </nav>
    <br/>

    <ul>
        <li>Total des joueurs : {{ totalUsers }}</li>
        <li>Total des tournois : {{ totalTournaments }}</li>
        <li>Total des inscriptions : {{ totalRegistrations }}</li>
        <li>Total des matchs : {{ totalMatches }}</li>
    </ul>

{% endblock %}
