<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% endblock %}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 1000px;
            margin: auto;
            padding: 2rem;
            background-color: #0B0B0D;
            color: #F6F7FF;
        }

        nav {
            background-color: #181818;
            padding: 1rem;
            margin-bottom: 2rem;
            border-radius: 8px;
        }

        nav a {
            color: #A9A9BD;
            text-decoration: none;
            margin-right: 1rem;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            transition: background-color 0.2s, color 0.2s;
        }

        nav a:hover {
            background-color: #A9A9BD;
            color: #181818;
        }

        nav a.active {
            background-color: #A9A9BD;
            color: #181818;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 1rem;
            background-color: #181818;
            color: #F6F7FF;
            table-layout: auto;
        }

        th, td {
            border: 1px solid #8D8D8D;
            padding: 8px;
            text-align: left;
            vertical-align: middle;
        }

        th {
            background-color: #A9A9BD;
            color: #181818;
        }

        th.actions, td.actions {
            white-space: nowrap;
            min-width: 150px;
            max-width: 200px;
            text-align: center;
        }

        .actions a {
            display: inline-block;
            margin-right: 0.5rem;
        }

        a:not(nav a) {
            color: #A9A9BD;
            text-decoration: none;
            margin-right: 0.5rem;
            transition: color 0.2s;
        }

        a:not(nav a):hover {
            color: #F6F7FF;
        }

        .flash-success {
            background: #d4edda;
            color: #155724;
            padding: 0.5rem;
            margin: 1rem 0;
            border: 1px solid #c3e6cb;
        }

        .back-link {
            display: inline-block;
            margin-top: 1rem;
            color: #A9A9BD;
            text-decoration: underline;
        }

        .back-link:hover {
            color: #F6F7FF;
        }

        form div {
            margin-bottom: 0.5rem;
        }

        form label {
            display: block;
            margin-bottom: 0.3rem;
            font-weight: bold;
            color: #F6F7FF;
        }

        form > div {
            margin-bottom: 1rem;
        }

        form div.checkbox {
            margin-bottom: 1rem;
        }

        .checkbox {
            display: block;
            margin-bottom: 1rem;
        }

        .checkbox > label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
            color: #F6F7FF;
        }

        .checkbox div {
            display: flex;
            flex-wrap: wrap;
        }

        .checkbox input[type="checkbox"] {
            width: auto;
            margin-right: 0.5rem;
        }

        .checkbox label.checkbox-inline {
            display: inline-flex;
            align-items: center;
            margin-right: 2rem;
            font-weight: normal;
            cursor: pointer;
        }

        input, select, textarea {
            background-color: #181818;
            color: #F6F7FF;
            border: 1px solid #A9A9BD;
            padding: 0.6rem;
            width: 100%;
            border-radius: 4px;
            font-size: 1rem;
        }

        button {
            background-color: #A9A9BD;
            color: #181818;
            border: none;
            padding: 0.7rem 1.2rem;
            cursor: pointer;
            font-weight: bold;
            border-radius: 4px;
            transition: background-color 0.2s, color 0.2s;
        }

        button:hover {
            background-color: #F6F7FF;
            color: #181818;
        }

        input, select, textarea {
            background-color: #181818;
            color: #F6F7FF;
            border: 1px solid #A9A9BD;
            padding: 0.4rem;
            width: 100%;
        }

        label {
            display: block;
            margin-bottom: 0.3rem;
            color: #A9A9BD;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }
        
        .stat-card {
            background-color: #1E1E24;
            border-radius: 8px;
            padding: 20px;
            display: flex;
            align-items: center;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }
        
        .card-icon {
            font-size: 2rem;
            margin-right: 15px;
            opacity: 0.8;
        }
        
        .card-content {
            flex-grow: 1;
        }
        
        .card-value {
            font-size: 2.2rem;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .card-label {
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 0.9rem;
            opacity: 0.7;
        }
        
        .players {
            border-left: 4px solid #4E6E91;
            box-shadow: 0 0 15px rgba(78, 110, 145, 0.15);
        }
        
        .tournaments {
            border-left: 4px solid #9C5D91;
            box-shadow: 0 0 15px rgba(156, 93, 145, 0.15);
        }
        
        .registrations {
            border-left: 4px solid #5E8969;
            box-shadow: 0 0 15px rgba(94, 137, 105, 0.15);
        }
        
        .matches {
            border-left: 4px solid #BA7967;
            box-shadow: 0 0 15px rgba(186, 121, 103, 0.15);
        }
        
        .card-link {
            text-decoration: none;
            color: inherit;
            display: block;
        }
        
        .card-link:hover {
            text-decoration: none;
            color: inherit;
        }
        
        .card-link .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }
        
        .add-button {
            display: inline-block;
            background-color: #A9A9BD;
            color: #181818;
            border: none;
            padding: 0.7rem 1.2rem;
            margin-top: 1rem;
            font-weight: bold;
            border-radius: 4px;
            text-decoration: none;
            transition: background-color 0.2s, color 0.2s;
            cursor: pointer;
        }
        
        .add-button:hover {
            background-color: #F6F7FF;
            color: #181818;
        }
    </style>
</head>

<body>
    <nav>
        <a href="{{ path('admin_dashboard') }}" class="{{ app.request.attributes.get('_route') == 'admin_dashboard' ? 'active' : '' }}">Dashboard</a>
        <a href="{{ path('admin_players_index') }}" class="{{ app.request.attributes.get('_route') == 'admin_players_index' ? 'active' : '' }}">Joueurs</a> 
        <a href="{{ path('admin_tournaments_index') }}" class="{{ app.request.attributes.get('_route') == 'admin_tournaments_index' ? 'active' : '' }}">Tournois</a> 
        <a href="{{ path('admin_registrations_index') }}" class="{{ app.request.attributes.get('_route') == 'admin_registrations_index' ? 'active' : '' }}">Inscriptions</a> 
        <a href="{{ path('admin_sportmatches_index') }}" class="{{ app.request.attributes.get('_route') == 'admin_sportmatches_index' ? 'active' : '' }}">Matchs</a>
    </nav>

    {% for message in app.flashes('success') %}
        <div class="flash-success">{{ message }}</div>
    {% endfor %}

    {% block body %}{% endblock %}
</body>
</html>
