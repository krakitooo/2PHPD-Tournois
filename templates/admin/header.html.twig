<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% endblock %}</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 800px; margin: auto; padding: 1rem; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 1rem; }
        th, td { border: 1px solid #ddd; padding: 8px; }
        th { background: #f4f4f4; }
        a { color: blue; text-decoration: none; margin-right: 0.5rem; }
        .flash-success { background: #d4edda; color: #155724; padding: 0.5rem; margin: 1rem 0; border: 1px solid #c3e6cb; }
        form div { margin-bottom: 0.5rem; }
        button { padding: 0.5rem 1rem; }
    </style>
</head>
<body>
    {% for message in app.flashes('success') %}
        <div class="flash-success">{{ message }}</div>
    {% endfor %}

    {% block body %}{% endblock %}
</body>
</html>
